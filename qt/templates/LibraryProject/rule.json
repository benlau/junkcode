{
    "ignorePattern": [".*swp", "vendor","user$", ".DS_Store", "qmlc$","build-"],
    "pathReplace": [
        {
            "find": "MYPROJECT",
            "replace": "%{ProjectName}"
        }
    ],
    "fileReplace": [
        {
            "pattern": ".*",
            "find": "MYPROJECT",
            "replace": "%{ProjectName}"
        },
        {
            "pattern": ".*",
            "find": "\\\\",
            "replace": "\\\\"
        }
    ]
}
